<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{restaurant.name}} Menu</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }
        .header {
            background-color: #e0e0e0;
            padding: 20px;
            text-align: center;
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 1000;
        }
        .header h1 {
            margin: 0;
            font-size: 2em;
        }
        .header p {
            margin: 5px 0;
        }
        .nav {
            background-color: #333;
            overflow: hidden;
            display: flex;
            justify-content: space-around;
            position: fixed;
            top: 100px; /* Adjust based on header height */
            width: 100%;
            z-index: 1050;
        }
        .nav a {
            color: white;
            padding: 14px 20px;
            text-align: center;
            text-decoration: none;
            display: block;
        }
        .nav a:hover {
            background-color: #575757;
        }
        .nav .active {
            background-color: #0000FF;
        }
        .container {
            padding: 160px 20px 20px; /* Add padding top to accommodate fixed header and nav */
            max-width: 1200px;
            margin: auto;
        }
        .menu-category {
            margin-bottom: 20px;
        }
        .menu-category h2 {
            border-bottom: 2px solid #333;
            padding-bottom: 5px;
        }
        .menu-item {
            display: flex;
            align-items: center;
            margin: 10px 0;
            padding: 10px;
            background-color: white;
            border: 1px solid #ccc;
        }
        .menu-item img {
            width: 150px;
            height: 100px;
            object-fit: cover;
            margin-right: 20px;
        }
        .menu-item h3 {
            margin: 0;
            font-size: 1.2em;
        }
        .menu-item p {
            margin: 5px 0;
        }
        .menu-item .price {
            font-weight: bold;
            margin-bottom: 5px;
        }
        .footer {
            text-align: center;
            padding: 20px;
            background-color: #e0e0e0;
            margin-top: 20px;
        }
        .footer p {
            margin: 5px 0;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>{{restaurant.name}}</h1>
        <p>{{restaurant.phone}} | <a href="http://{{restaurant.website}}" target="_blank">{{restaurant.website}}</a> | <a href="mailto:{{restaurant.email}}">{{restaurant.email}}</a></p>
    </div>
    <div class="nav">
        <a href="#main" class="active">MAIN</a>
        <a href="#starters">STARTERS</a>
        <a href="#desserts">DESSERTS</a>
        <a href="#drinks">DRINKS</a>
    </div>
    <div class="container">
        <div id="main" class="menu-category">
            <h2>Main</h2>
            {{#each menu.mains}}
                <div class="menu-item">
                    <img src="path/to/your/image.jpg" alt="{{this.name}}">
                    <div>
                        <h3>{{this.name}}</h3>
                        <p class="price">{{this.price}}</p>
                        <p>{{this.description}}</p>
                    </div>
                </div>
            {{/each}}
        </div>

        <div id="starters" class="menu-category">
            <h2>Starters</h2>
            {{#each menu.starters}}
                <div class="menu-item">
                    <img src="path/to/your/image.jpg" alt="{{this.name}}">
                    <div>
                        <h3>{{this.name}}</h3>
                        <p class="price">{{this.price}}</p>
                        <p>{{this.description}}</p>
                    </div>
                </div>
            {{/each}}
        </div>

        <div id="desserts" class="menu-category">
            <h2>Desserts</h2>
            {{#each menu.desserts}}
                <div class="menu-item">
                    <img src="path/to/your/image.jpg" alt="{{this.name}}">
                    <div>
                        <h3>{{this.name}}</h3>
                        <p class="price">{{this.price}}</p>
                        <p>{{this.description}}</p>
                    </div>
                </div>
            {{/each}}
        </div>

        <div id="drinks" class="menu-category">
            <h2>Drinks</h2>
            {{#each menu.drinks}}
                <div class="menu-item">
                    <img src="path/to/your/image.jpg" alt="{{this.name}}">
                    <div>
                        <h3>{{this.name}}</h3>
                        <p class="price">{{this.price}}</p>
                        <p>{{this.description}}</p>
                    </div>
                </div>
            {{/each}}
        </div>
    </div>

    <div class="footer">
        <p>Contact us at: <a href="mailto:{{restaurant.email}}">{{restaurant.email}}</a> | {{restaurant.phone}}</p>
        <p>Visit our website: <a href="http://{{restaurant.website}}" target="_blank">{{restaurant.website}}</a></p>
    </div>
</body>
</html>
